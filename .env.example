# Mapbox Configuration
# Get your access token from: https://account.mapbox.com/access-tokens/
# Create a token with the following scopes: DOWNLOADS:READ (for SDK), and default public scopes
EXPO_PUBLIC_MAPBOX_ACCESS_TOKEN=your_mapbox_public_access_token_here

# For native builds, you also need a download token with DOWNLOADS:READ scope
# Set this as an environment variable before running: npx expo prebuild
# export RNMAPBOX_MAPS_DOWNLOAD_TOKEN=your_mapbox_download_token_here
